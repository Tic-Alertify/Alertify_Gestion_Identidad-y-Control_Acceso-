<?xml version="1.0" encoding="utf-8"?>
<!--
    Configuración de seguridad de red para desarrollo.
    Permite tráfico HTTP (cleartext) SOLO hacia 10.0.2.2 (localhost del PC desde emulador)
    y localhost. En producción, usar HTTPS exclusivamente.
-->
<network-security-config>
    <!-- Por defecto: solo HTTPS -->
    <base-config cleartextTrafficPermitted="false" />

    <!-- Excepciones para desarrollo local -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
